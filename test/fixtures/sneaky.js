var o = {};
window[x] = 5;
Object.assign(window, other);
Object.assign(Foo.prototype, { m() { return 1; } });
self.importScripts('worker-lib.js');
var r = window.eval('2+2');
setTimeout("alert(1)", 0);
var req = require('./thing');
for (i = 0; i < 2; i++) { console.log(i); }
